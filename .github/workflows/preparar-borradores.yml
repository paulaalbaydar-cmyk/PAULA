name: Preparar el consejo de la semana

on:
  schedule:
    # Domingo 16:07 UTC: deja listo el consejo del martes para revisarlo
    # (la noticia del jueves se redacta el mismo jueves para que sea actual)
    - cron: "7 16 * * 0"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: solareia-redes
  cancel-in-progress: false

jobs:
  preparar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Generar borradores con Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python -m solareia_social.main generate
          python -m solareia_social.main preview
      - uses: actions/upload-artifact@v4
        with:
          name: vista-previa-borradores
          path: previews/
      - name: Guardar borradores
        run: |
          git config user.name "solareia-bot"
          git config user.email "solareia-bot@users.noreply.github.com"
          git add content/queue
          git diff --cached --quiet || git commit -m "Borradores de redes para la semana"
          git push
